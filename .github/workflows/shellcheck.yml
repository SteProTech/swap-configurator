name: Code Validation

on: [push, pull_request]

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Validate Script
      uses: ludeeus/action-shellcheck@1.2.0
      with:
        path: "src/*.sh"
